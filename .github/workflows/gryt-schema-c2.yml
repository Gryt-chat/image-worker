name: Gryt trigger schema C2
on:
  push:
    branches: [fix/gryt-git-notify-events]
  discussion:
    types: [created, answered, unanswered]
  discussion_comment:
    types: [created]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
